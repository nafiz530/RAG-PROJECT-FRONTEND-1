// Grok-optimized Next.js config: Sleek, secure, and primed for Vercel deployment with App Router efficiency.

/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true, // Enables React's Strict Mode for better debugging and performance.
  swcMinify: true, // Uses SWC for faster minification.
  // Add future configurations here for images, env, or Supabase integrations as needed.
};

export default nextConfig;